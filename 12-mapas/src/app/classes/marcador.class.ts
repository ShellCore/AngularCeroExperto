export class Marcador {

    constructor(
        public lat: number,
        public lng: number,
        public title: string = "Sin título",
        public desc: string = "Sin descripción"
    ) {

    }
}